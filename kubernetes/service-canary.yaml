apiVersion: v1
kind: Service
metadata:
  name: demo-app-canary
  namespace: demo-app
  labels:
    app: demo-app
    role: canary
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: demo-app
    role: canary

# Made with Bob
