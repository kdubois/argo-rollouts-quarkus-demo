apiVersion: v1
kind: Service
metadata:
  name: demo-app-stable
  namespace: demo-app
  labels:
    app: demo-app
    role: stable
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: demo-app
    role: stable

# Made with Bob
